# =====================================================
# NOTIFICATION SERVICE - Perfil LOCAL (Desarrollo)
# =====================================================

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true


logging:
  level:
    root: INFO
    dev.codebymelendez: DEBUG
    org.springframework.amqp.rabbit: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
